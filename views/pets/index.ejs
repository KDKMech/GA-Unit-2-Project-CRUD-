<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Pets page</title>
    <link rel="stylesheet" type="text/css" href="/CSS/styles.css">
</head>
<body>
    <h1>here are all the pets posted</h1>
    <a href="/pets/addPet"><button id="addPet">add a Pet</button></a>
    <ul>
        <% pets.forEach(pet => { %>
           <li><a href="/pets/<%= pet._id %>"><%= pet.name %></a></li> 
        <% }); %>
    </ul>
</body>
</html>